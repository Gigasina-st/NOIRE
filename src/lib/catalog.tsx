import { createContext, useContext, useEffect, useState, type ReactNode } from 'react';
import { supabase } from './supabase'; import { products as fallbackProducts, type Product } from '../data';
type DbProduct={id:string;name:string;slug:string;category:string;price:number;currency:string;description:string|null;details:string[]|null;image:string|null;gallery:string[]|null;stock:number|null;sizes:string[]|null;colors:string[]|null};
const CatalogContext=createContext<Product[]>(fallbackProducts);
function toProduct(row:DbProduct):Product{const fallback=fallbackProducts.find(product=>product.name===row.name);return{id:row.id,name:row.name,category:row.category,price:new Intl.NumberFormat('en-US',{style:'currency',currency:row.currency||'EUR',maximumFractionDigits:0}).format(Number(row.price)),image:row.image||fallback?.image||'',alt:fallback?.alt||row.name,gallery:row.gallery?.length?row.gallery:fallback?.gallery||[],description:row.description||fallback?.description||'',details:row.details?.length?row.details:fallback?.details||[],stock:row.stock??fallback?.stock,sizes:row.sizes?.length?row.sizes:fallback?.sizes,colors:row.colors?.length?row.colors:fallback?.colors}}
export function CatalogProvider({children}:{children:ReactNode}){const[catalog,setCatalog]=useState<Product[]>(fallbackProducts);useEffect(()=>{let active=true;async function loadProducts(){const{data,error}=await supabase.from('products').select('id,name,slug,category,price,currency,description,details,image,gallery,stock,sizes,colors').eq('active',true).order('created_at',{ascending:true});if(error){console.warn('NOIRÉ: Could not load products from Supabase. Using local fallback.',error.message);return}if(active&&data?.length)setCatalog((data as DbProduct[]).map(toProduct))}loadProducts();return()=>{active=false}},[]);return <CatalogContext.Provider value={catalog}>{children}</CatalogContext.Provider>}
export function useProducts(){return useContext(CatalogContext)}